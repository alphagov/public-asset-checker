<p style="color: green"><%= notice %></p>

<%= render @public_asset %>

<%= render "line_chart", labels: @labels.to_json, url: @url.to_json, values: @values %>

<div>
  <%= link_to "Edit", edit_public_asset_path(@public_asset) %>
  <%= button_to "Destroy", @public_asset, method: :delete %>
</div>
